## Artifacts

Runtime outputs are organized here:

- `data/`: generated datasets and rollout outputs.
- `models/`: trained model checkpoints and exported formats.

Existing model files were moved from the repository root to keep the top-level folder clean.
